image: {{.Image}}

variables:
  {{range $key, $value := .Variables}}
  {{$key}}: "{{$value}}"
  {{end}}

services:
  {{range .Services}}
  - {{.}}
  {{end}}

stages:
  {{range .Stages}}
  - {{.}}
  {{end}}

cache:
  paths:
    {{range .CachePaths}}
    - {{.}}
    {{end}}

before_script:
  {{range .BeforeScript}}
  - {{.}}
  {{end}}

build:
  stage: build
  script:
    - {{.BuildCommand}}
  artifacts:
    paths:
      - venv/
    expire_in: 1 hour

test:
  stage: test
  dependencies:
    - build
  script:
    - python manage.py migrate --settings=settings.test
    - {{.TestCommand}}
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
    paths:
      {{range .ArtifactPaths}}
      - {{.}}
      {{end}}
    expire_in: 1 week
    when: always
  coverage: '/TOTAL.+?(\d+\%)$/'